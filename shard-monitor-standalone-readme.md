# shard-monitor-standalone 面板字段说明

`shard-monitor-standalone.sh` 单目录模式面板示例:

```
runid                  done/total   %      work    queue   爬取
1700000000-12345       30/100       30.0   2       5       已定稿
本次 run 总量: 250000 文件 / 50.0G
```

下面解释 `爬取/已定稿`、`work`、`queue` 等字段的含义。这些词都来自 fpsync
的内部工作机制,先讲它怎么干活,这些词就好懂了。

## fpsync 的工作流

fpsync 同步时分两个角色协作:

1. **FPART(爬虫)**:遍历源目录,把海量文件**切成一个个"分区"(part)**。每个 part
   不是文件本身,而是"一份文件清单"——比如 part.1 列了 1000 个文件的路径,part.2 列了下一批,
   以此类推。
2. **QMGR(调度器)**:把这些 part 一个个派给 rsync worker 去传。`-n` 控制同时有几个
   rsync 在跑。

fpsync 用三个目录来记录每个 part 的状态,一个 part 会依次流过这三个目录:

```
分区生成 → queue(排队) → work(运行) → done(完成)
   ↑FPART        └──────── QMGR 调度 ─────────┘
```

## 三个计数的含义

- **queue(排队中)**:FPART 已经切好、但还没轮到传的分区。worker 忙不过来时它们在这排队等。
- **work(运行中)**:**此刻正在被 rsync 传输**的分区。这个数约等于你的真实并发度,正常应该
  接近 `-n` 设定值;如果 work 长期远小于 `-n`,说明 worker 没喂满(可能 FPART 爬得慢,
  或源端读太慢)。
- **done(已完成)**:已经传完的分区。进度百分比 = done / total。

举例:`done=30, work=2, queue=5`,意思是 100 个分区里 30 个传完了、2 个正在传、5 个排队
等着,剩下的还没被 FPART 切出来。

## "爬取 / 已定稿" 是什么

这说的是 **FPART 这个爬虫有没有把源目录遍历完**。

fpsync 默认开 **live mode(边爬边传)**:不等全部扫描完就开始派活。好处是快,但副作用是
——**在爬完之前,分区总数 `total` 是会一直涨的**。

- **爬取中**:FPART 还在遍历源目录,新的 part 还在不断冒出来,`total` 没定下来。这时候的
  百分比(done/total)是"假"的——分母还在变大,你看到 30% 过一会儿可能掉回 20%,因为
  total 涨了。所以面板会标 `*` 提醒你别当真。
- **已定稿**:FPART 已经爬完整个源目录(内部出现一个 `fp_done` 标志文件),分区总数 `total`
  锁定不再变。**这之后的百分比才真正可信**,能据它估进度。

所以"已定稿"≈"源端文件清单已全部生成完毕,总量已确定"。判断依据就是 `queue/` 或 `work/`
目录下有没有那个 `fp_done` 标志文件,面板的"爬取"列就是在检测它。

## 字段速查表

| 字段 | 含义 | 数据来源 |
|---|---|---|
| `runid` | 本次 fpsync run 的标识(形如 `<epoch>-<pid>`) | `parts/` 下子目录名 |
| `done/total` | 已完成分区数 / 总分区数 | `done/` 文件数 / `run.meta` 的 `total_num_parts` |
| `%` | 进度百分比 = done / total | 计算得出 |
| `work` | 正在传输的分区数(≈真实并发度) | `work/` 下的 part 文件数 |
| `queue` | 排队待传的分区数 | `queue/` 下的 part 文件数 |
| `爬取` | FPART 是否爬完(`已定稿` / `爬取中*`) | `queue/`或`work/` 下有无 `fp_done` |
| `本次 run 总量` | 源端总文件数 / 总字节数 | `run.meta` 的 `total_num_files` / `total_size` |

> 注:`爬取中*` 时百分比仅供参考,因为 `total` 还会随 FPART 爬取继续增长。
